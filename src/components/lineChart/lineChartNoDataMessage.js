import React from 'react';

const lineChartNoDataMessage = ({message}) => (
    <div className="vertical-center justify-content-center text-muted">
        {message}
    </div>
);

export default lineChartNoDataMessage;
import React from 'react';

const scatterPlotNoDataMessage = () => (
    <div className="vertical-center justify-content-center text-muted">
        Please select columns and generate the chart so it can be displayed.
    </div>
);

export default scatterPlotNoDataMessage;
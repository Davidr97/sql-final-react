import React from 'react';

const pieChartNoDataMessage = () => (
    <div className="text-muted">
            Please select columns and generate the chart so it can be displayed.
    </div>
);

export default pieChartNoDataMessage;